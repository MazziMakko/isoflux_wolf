# =====================================================
# ISOFLUX PRODUCTION @ https://www.isoflux.app
# Copy to .env.production on server (never commit real file)
# =====================================================

# Application (required for emails, redirects, API callbacks)
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_APP_URL=https://www.isoflux.app

# Supabase (same project as dev; use production keys if separate)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Database (migrations from server if needed)
DATABASE_URL=

# Stripe (use live keys in production)
STRIPE_SECRET_KEY=sk_live_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_STARTER=price_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_ENTERPRISE=price_...

# Security (generate with: openssl rand -base64 32)
JWT_SECRET=min_32_chars_for_production
ENCRYPTION_KEY=exactly_32_characters_for_aes
CRON_SECRET=for_retention_cron

# Optional: Redis, HSM, mTLS, Sentry (see docs/DEPLOYMENT.md)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# SENTRY_DSN=
